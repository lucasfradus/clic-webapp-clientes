# Base URL del backend CLIC Pilates.
# En dev, dejar relativa (/api/v1) para usar el proxy de Vite (ver vite.config.ts).
# En producción (Railway), setear la URL absoluta:
#   VITE_API_BASE_URL=https://app.clicpilates.com/api/v1
VITE_API_BASE_URL=/api/v1
